# ShiJu 系统稳定化计划

**创建时间：** 2026-07-06  
**目标：** 让单用户可以毫无障碍地体验和使用所有功能，并可长期稳定使用  
**前提：** 单用户场景，无需用户认证，中转API已配置

---

## 当前状态快照（2026-07-06 更新）

### ✅ 已完成
- 前端 Web 已部署（http://43.128.11.119/shiju/）
- PostgreSQL 数据库 schema 完整（8张表）
- 中转 API 已配置（AI_PROVIDER=openai_compatible）
- 核心功能代码已实现（文章导入、分段、复习、SRS）
- Git 仓库干净（无未提交变更）
- **API 服务稳定运行**（systemd + EnvironmentFile，自动重启）
- **所有 API 测试通过**（21/21）
- **/health 健康检查端点**（数据库连接验证）
- **端到端验证通过**（导入→复习→状态更新）
- **每日数据库备份**（2 AM 自动运行，30天保留）
- **日志查看工具**（shiju-logs.sh）
- **性能监控工具**（shiju-perf.sh）
- **数据库索引优化**（review queue 复合索引）

### ⚠️ 已知问题
- 前端测试需要 API mocking 重构（不影响实际运行）
- 数据库约束已修复（feedback, mastery_status）
- NaN 值处理已修复（ease_factor, interval_days）

### 📊 当前数据
- 86 张表达式卡片（85 张待复习）
- 30 篇文章，45 个分段
- 数据库大小：8.7MB
- API 响应时间：9-11ms

---

## 问题分类

### P0 - 系统不可用（必须立即修复）

**1. API 服务未启动**
- 现象：curl localhost:3001 失败
- 影响：前端无法调用任何后端接口
- 修复：启动服务 + 配置 systemd 持久运行

**2. 测试数据库配置错误**
- 现象：3个 API 测试失败 - "SASL: client password must be a string"
- 影响：无法验证数据库操作正确性
- 修复：配置测试环境数据库连接

**3. 前端测试路由配置错误**
- 现象：5个前端测试失败 - Router basename 不匹配
- 影响：无法验证前端逻辑
- 修复：调整测试用例使用 MemoryRouter

### P1 - 功能不完整（影响使用体验）

**4. 缺少健康检查端点**
- 现象：GET /health 返回 404
- 影响：无法监控服务状态
- 修复：添加 /health 端点

**5. Prompt 生成测试失败**
- 现象：测试期望包含 'local_meaning' 但实际 prompt 不含
- 影响：无法验证 AI 生成质量
- 修复：更新测试用例或修复 prompt 生成逻辑

**6. 无端到端验证**
- 现象：未验证完整的导入→分段→生成→复习流程
- 影响：不确定系统是否真正可用
- 修复：手动验证关键路径

### P2 - 长期稳定性（不阻塞使用）

**7. 缺少错误监控**
- 现象：API 错误只在日志中，无聚合和告警
- 影响：问题发现不及时
- 修复：添加基础日志查看工具

**8. 缺少数据备份**
- 现象：无定期数据库备份
- 影响：数据丢失风险
- 修复：配置每日备份脚本

---

## 执行计划（按优先级）

### Phase 1: 让系统跑起来（今天完成）

#### Task 1.1: 启动 API 服务 ⚡
**目标：** API 服务持久运行，前端可调用

**步骤：**
1. 验证 .env 配置完整（DATABASE_URL, AI_PROVIDER, AI_BASE_URL, AI_API_KEY, AI_MODEL）
2. 测试启动：`pnpm dev:api`
3. 验证健康（curl localhost:3001，虽然会 404 但证明服务运行）
4. 配置 systemd 服务持久运行

**验收标准：**
- [ ] `curl http://localhost:3001/articles` 返回 JSON（即使是空数组或错误，证明服务响应）
- [ ] 服务重启后自动恢复
- [ ] 前端可以调用 API（浏览器 Network 面板无 CORS 或连接错误）

**预计时间：** 30 分钟

---

#### Task 1.2: 修复测试数据库配置 ⚡
**目标：** API 测试通过，验证数据库操作正确

**步骤：**
1. 检查 `apps/api/src/config.ts` 的测试环境配置
2. 创建测试数据库或配置测试环境使用主数据库
3. 修复密码格式问题（确保 DATABASE_URL 中密码是字符串）
4. 运行测试：`pnpm test:api`

**验收标准：**
- [ ] `articles.test.ts` 全部通过（3个测试）
- [ ] `app.test.ts` 导入测试通过（1个测试）
- [ ] 无数据库连接错误

**预计时间：** 30 分钟

---

#### Task 1.3: 修复前端测试路由配置 ⚡
**目标：** 前端测试通过，验证 UI 逻辑正确

**步骤：**
1. 修改 `apps/web/src/App.test.tsx`
2. 使用 `MemoryRouter` 替代 `BrowserRouter`
3. 设置 `initialEntries={['/shiju/']}` 匹配 basename
4. 运行测试：`pnpm test:web`

**验收标准：**
- [ ] 5个 App 测试全部通过
- [ ] 无 Router basename 错误

**预计时间：** 20 分钟

---

### Phase 2: 补全缺失功能（今天/明天完成）

#### Task 2.1: 添加健康检查端点
**目标：** 可以监控服务状态

**步骤：**
1. 在 `apps/api/src/app.ts` 添加 `GET /health` 路由
2. 返回：`{ status: 'ok', timestamp: Date.now(), database: 'connected' }`
3. 测试数据库连接并返回状态

**验收标准：**
- [ ] `curl http://localhost:3001/health` 返回 200 和 JSON
- [ ] 包含数据库连接状态

**预计时间：** 15 分钟

---

#### Task 2.2: 修复 Prompt 生成测试
**目标：** AI 生成逻辑测试通过

**步骤：**
1. 检查 `apps/api/src/ai/openAiCompatibleProvider.ts` 的 prompt 生成
2. 确认是否需要包含 'local_meaning' 字段
3. 更新测试用例或修复 prompt

**验收标准：**
- [ ] `openAiCompatibleProvider.test.ts` 通过
- [ ] Prompt 包含所有必需字段

**预计时间：** 15 分钟

---

#### Task 2.3: 端到端验证
**目标：** 验证核心功能真实可用

**手动测试清单：**

1. **文章导入流程**
   - [ ] 访问 http://43.128.11.119/shiju/articles
   - [ ] 粘贴测试文本（200词英文段落）
   - [ ] 点击导入
   - [ ] 检查是否成功（成功提示 + 跳转）
   - [ ] 查看数据库：`SELECT * FROM articles ORDER BY created_at DESC LIMIT 1;`
   - [ ] 查看分段：`SELECT * FROM segments WHERE article_id = '<刚导入的ID>';`

2. **AI 生成流程**
   - [ ] 检查 segments 表的 generation_status
   - [ ] 等待后台生成完成（或检查日志）
   - [ ] 查看候选表达式：`SELECT * FROM candidate_expressions WHERE segment_id = '<某个segment>';`
   - [ ] 验证有 selected 状态的候选

3. **阅读流程**
   - [ ] 访问阅读页面
   - [ ] 查看高亮表达式
   - [ ] 点击查看释义
   - [ ] 添加到复习队列

4. **复习流程**
   - [ ] 访问复习页面
   - [ ] 查看到期卡片
   - [ ] 提交反馈（不知道/知道）
   - [ ] 检查 review_logs 表记录

**验收标准：**
- [ ] 所有4个流程无错误完成
- [ ] 数据库记录正确
- [ ] 前端显示正常

**预计时间：** 1 小时

---

### Phase 3: 长期稳定性（本周完成）

#### Task 3.1: 配置数据库备份
**目标：** 每日自动备份，防止数据丢失

**步骤：**
1. 创建备份脚本 `scripts/backup-db.sh`
2. 使用 `pg_dump` 导出到 `~/ShiJu/backups/`
3. 配置 cron 每日凌晨3点执行
4. 保留最近7天备份

**验收标准：**
- [ ] 备份脚本可手动执行
- [ ] cron 自动运行
- [ ] 备份文件正确生成

**预计时间：** 30 分钟

---

#### Task 3.2: 添加基础日志查看
**目标：** 方便排查问题

**步骤：**
1. 配置 Pino 日志输出到文件（`logs/api.log`）
2. 添加日志轮转（按天或按大小）
3. 创建查看脚本 `scripts/view-logs.sh`

**验收标准：**
- [ ] 日志持久化到文件
- [ ] 可以快速查看最近错误
- [ ] 日志不会无限增长

**预计时间：** 30 分钟

---

#### Task 3.3: 性能优化检查
**目标：** 确保长期使用不变慢

**检查清单：**
- [ ] 数据库索引是否合理（user_id, article_id, segment_id）
- [ ] 是否有慢查询（N+1问题）
- [ ] API 响应时间是否正常（<500ms）
- [ ] 前端打包体积是否合理（<2MB）

**如发现问题，优化：**
- 添加缺失索引
- 优化查询（使用 JOIN 代替多次查询）
- 添加适度缓存

**预计时间：** 1 小时

---

## 完整验收标准

### 功能完整性 ✅
- [ ] 可以导入文章（TXT/Markdown）
- [ ] 自动分段（150-250词）
- [ ] AI 生成候选表达式（使用中转API）
- [ ] 阅读原文并查看高亮释义
- [ ] 手动选择表达式生成卡片
- [ ] 语境卡片生成（输入场景+表达式）
- [ ] SRS 复习系统（到期提醒、反馈按钮）
- [ ] 卡片库查看所有学习内容

### 系统稳定性 ✅
- [ ] API 服务持久运行（systemd）
- [ ] 数据库每日备份
- [ ] 所有测试通过（18/18）
- [ ] 无内存泄漏（长期运行）
- [ ] 错误日志可查看

### 用户体验 ✅
- [ ] 前端加载速度快（<3秒）
- [ ] API 响应快（<500ms）
- [ ] 操作流畅无卡顿
- [ ] 错误提示清晰
- [ ] 数据不会丢失

---

## 技术债务记录

### 当前已知但接受的技术债务

1. **硬编码 userId = "user-1"**
   - 原因：单用户场景，无需认证
   - 影响：多用户扩展需要重构
   - 接受理由：当前需求不需要

2. **Mock Provider 仍存在**
   - 原因：测试需要
   - 影响：无
   - 接受理由：测试必需

3. **前端无错误边界**
   - 原因：开发阶段未配置
   - 影响：前端崩溃用户体验差
   - 计划：Phase 3 添加

4. **无 API 限流**
   - 原因：单用户无需
   - 影响：理论上可被滥用
   - 接受理由：单用户场景风险低

---

## 风险评估

### 高风险（需要密切关注）
- **中转 API 稳定性**：依赖第三方服务，可能下线或限流
  - 缓解：添加重试机制，记录失败日志
  
- **数据库无备份**：当前无自动备份，数据丢失风险高
  - 缓解：Task 3.1 立即执行

### 中风险（可接受）
- **单点故障**：API 服务挂掉整个系统不可用
  - 缓解：systemd 自动重启
  
- **无监控告警**：问题发现不及时
  - 缓解：定期查看日志

### 低风险
- **性能问题**：单用户场景数据量小
- **安全问题**：无公网暴露，仅本地使用

---

## 时间估算

- **Phase 1（让系统跑起来）：** 1.5 小时
- **Phase 2（补全功能）：** 2 小时
- **Phase 3（长期稳定性）：** 2 小时

**总计：** 5.5 小时（分2天完成）

---

## 成功标准

**Day 1 结束时：**
- API 服务运行
- 所有测试通过
- 可以导入文章并看到高亮

**Day 2 结束时：**
- 完整流程验证通过
- 数据库备份配置
- 日志可查看

**最终交付时：**
- 用户可以顺畅使用所有功能
- 系统可以稳定运行1个月不出问题
- 出问题时可以快速定位和恢复

---

## 下一步行动

**立即开始：** Task 1.1 - 启动 API 服务

**命令：**
```bash
cd ~/ShiJu
pnpm dev:api  # 先手动测试
```

如果成功，继续配置 systemd 持久运行。

---

**文档版本：** v1.0  
**最后更新：** 2026-07-06  
**负责人：** Orion
